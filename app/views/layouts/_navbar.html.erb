<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Toy App",root_path,id:"logo",class:"navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "All Post", microposts_path,style:"color:white!important;" %></li>
        <li><%= link_to "All User", users_path,style:"color:white!important;" %></li>
        <% if logged_in? %> <!-- if user not logged in --> 
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="color:white!important;">User Action <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "New Post", new_micropost_path %></li>
              <li><%= link_to "View Post", current_user %></li>
            </ul>
          </li>
          <% end %>
      </ul>

      <!--  -->
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %> <!-- if user not logged in -->
        <li><%= link_to 'log out', logout_path, method: :delete, style:"color:white!important;" %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="color:white!important;">User Profile <%= "Admin" if current_user.admin? %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View User Profile", current_user %></li>
            <li><%= link_to "Edit User Profile", edit_user_path(current_user) %></li>
          </ul>
          <% else %>
          <li><%= link_to 'login', login_path, method: :get, style:"color:white!important;" %></li>
          <li><%= link_to 'Sign up', signup_path, method: :get, style:"color:white!important;" %></li>
          <% end %>
        </li>
      </ul>
      <!--  -->
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>