<p id="notice"><%= notice %></p>


<div id="jumbotron1" class="center jumbotron">
  <h1>Users</h1>
  <%= link_to 'Sign Up', new_user_path ,class:"btn btn-primary btn-lg btn-xlarge" %>
</div>

<% @users.each do |user| %>
  <div class="row">
    <div class="col-xs-8 col-xs-offset-2">
      <div class="well well-lg">
        <div class="post-title"><%= link_to user.name, user %></div>
        <div class="post-body">
          <%= user.email %>
        </div>
        <div class="post-actions">
        <%= link_to 'Edit', edit_user_path(user) , class: "btn btn-xs btn-primary" %>
        <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger" ,style: "color:white !important;" %></td>
      </div>
    </div>
  </div>
</div>
<% end %>
 

  
